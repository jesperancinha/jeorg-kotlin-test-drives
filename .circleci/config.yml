version: 2.1

jobs:
  build-and-test:
    docker:
      - image: eclipse-temurin:21-alpine
    steps:
      - run: apk add maven make
      - checkout
      - run: make

workflows:
  sample:
    jobs:
      - build-and-test
